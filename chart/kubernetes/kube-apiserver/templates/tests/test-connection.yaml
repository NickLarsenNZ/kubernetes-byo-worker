apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-apiserver.fullname" . }}-test-connection"
  labels:
{{ include "kube-apiserver.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kube-apiserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
